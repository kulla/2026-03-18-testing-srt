# Repo for testing [@anthropic/sandbox-runtime](https://www.npmjs.com/package/@anthropic-ai/sandbox-runtime)

## Setup

1. Clone repo and run `npm install`
2. Go to one of the directories and run the scripts there
